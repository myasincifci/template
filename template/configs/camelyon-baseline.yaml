defaults:
    - base

logging: True

model:
    pretrained: False # SSL also not pretrained

data:
    name: camelyon
    num_classes: 2

trainer:
    max_epochs: 20
    check_val_every_n_epoch: 1

mixstyle:
    active: False

disc:
    active: False

logger:
    log_every_n_steps: 100
    project: supervised-experiments-camelyon